<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Profile | Amontron</title>
  <link rel="stylesheet" href="/stylesheets/profile.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
</head>
<body>
  <div class="profile-container">
    <div class="profile-card">
      <div class="avatar">
        <i class="fas fa-user-circle"></i>
      </div>
   <h1><%= user.name.toUpperCase() %></h1>


      <div class="profile-details">
        <p><i class="fas fa-venus-mars"></i> <strong>Gender:</strong> <%= user.gender %></p>
        <p><i class="fas fa-phone"></i> <strong>Mobile:</strong> <%= user.mobile %></p>
        <p><i class="fas fa-envelope"></i> <strong>Email:</strong> <%= user.email %></p>
      </div>

      <div class="button-group">
        <a href="/" class="btn home"><i class="fas fa-home"></i> Home</a>
        <a href="/logout" class="btn logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
      </div>
    </div>

    <div class="bookings-section">
      <h2><i class="fas fa-calendar-check"></i> Your Bookings</h2>
      <% if (bookings.length > 0) { %>
        <% bookings.forEach(booking => { %>
          <div class="booking-entry">
            <p><strong>Restaurant:</strong> <%= booking.restaurantName %></p>
            <p><strong>Date:</strong> <%= booking.bookingDate %> at <%= booking.timeSlot %></p>
            <p><strong>Guests:</strong> <%= booking.people %></p>
            <p><strong>Special Request:</strong> <%= booking.requests || "None" %></p>
          </div>
        <% }) %>
      <% } else { %>
        <p class="no-bookings">You haven't made any bookings yet.</p>
      <% } %>
    </div>
  </div>

  <footer class="site-footer">
    <div class="footer-content">
      <p>&copy; 2025 <strong>Amontron</strong> | Kolkata Restaurants | Taste the Tradition</p>
      <div class="footer-icons">
        <a href="/"><i class="fas fa-home"></i></a>
        <a href="#"><i class="fab fa-facebook-f"></i></a>
        <a href="#"><i class="fab fa-instagram"></i></a>
        <a href="#"><i class="fab fa-x-twitter"></i></a>
      </div>
    </div>
  </footer>
</body>
</html>
